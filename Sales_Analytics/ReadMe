--This project demonstrates functionalThis project demonstrates functional programming, custom stream operations, lambda expressions, and analytical processing on a sales dataset using Python. It includes modular components for data loading, transformation, stream-like operations, and business-level aggregations. programming, custom stream operations, lambda expressions, and analytical processing on a sales dataset using Python. It includes modular components for data loading, transformation, stream-like operations, and business-level aggregations.
